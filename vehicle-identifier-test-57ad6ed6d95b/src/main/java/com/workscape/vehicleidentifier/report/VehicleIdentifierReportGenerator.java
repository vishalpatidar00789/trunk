package com.workscape.vehicleidentifier.report;

import com.workscape.vehicleidentifier.bind.Vehicles;

public interface VehicleIdentifierReportGenerator {
	void generateReport(Vehicles vehicles);
}
